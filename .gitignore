node_modules/
test-results/
results/
playwright-report/
blob-report/
playwright/.cache/

__pycache__/
*.py[cod]
.venv/
venv/

target/
dbt_packages/
logs/

*.duckdb.wal
.env
.DS_Store

# Playwright
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/

*.duckdb

# The warehouse is committed on purpose: CI ingests into it and pushes it back,
# so the repo carries the run history and dashboards can open it with no server.
# Everything else DuckDB writes (WAL, scratch DBs) stays ignored above.
!warehouse/test_analytics.duckdb
